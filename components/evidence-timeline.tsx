import { timeline } from "@/lib/data"; import { Camera,FileCheck2,FileText,ShieldCheck,UserRoundCheck,Wrench } from "lucide-react";
const icons=[FileText,Camera,FileCheck2,Wrench,ShieldCheck,UserRoundCheck];
export function EvidenceTimeline(){return <div className="relative space-y-0 before:absolute before:bottom-5 before:left-[17px] before:top-5 before:w-px before:bg-[#D5DFEB]">{timeline.map(([time,title,type],i)=>{const Icon=icons[i];return <div key={title} className="relative flex gap-4 py-3"><span className={`z-10 grid size-9 shrink-0 place-items-center rounded-full border ${i===timeline.length-1?"border-[#4D8DFF] bg-[#EDF4FF] text-[#326DCA]":"border-[#D8E1EC] bg-white text-[#617189]"}`}><Icon size={15}/></span><div className="min-w-0 pt-0.5"><div className="flex flex-wrap items-baseline gap-x-2"><span className="font-mono text-[11px] text-[#748297]">{time}</span><p className="text-sm font-medium text-[#1A2739]">{title}</p></div><p className="mt-1 text-[11px] text-[#8591A1]">{type}</p></div></div>})}</div>}
